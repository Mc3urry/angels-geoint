"""Discrepancy events and the candidate queue.

PHASE 2.
    GET /events?bbox=&start=&end=&kind=&min_confidence=
    GET /events/{event_id}          # full dossier

DiscrepancyEvent.to_geojson already gives you the feature shape.
"""
