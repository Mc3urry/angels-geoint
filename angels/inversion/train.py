"""Train on the BuzzFeed labelled set.

PHASE 2.
97 confirmed federal surveillance aircraft plus a random sample of others.
Code and data are public: github.com/BuzzFeedNews/2017-08-spy-plane-finder

    def train(X, y) -> Model
    def evaluate(model, X, y) -> dict

They reported a 3.7% overall error rate with a random forest. That is a
specific published number to put yours against -- an evaluation section handed
to you.
"""
