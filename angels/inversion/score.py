"""Score aircraft and rank candidates.

PHASE 2.
    def score_all(tracks, model) -> list[Candidate]
    def to_geojson(candidates) -> dict          # feeds public/

This produces your November artifact. Publish the METHOD and the candidates
with explicit uncertainty. Never assert what an aircraft was doing --
overclaiming here is the one mistake that would follow you into interviews.
"""
